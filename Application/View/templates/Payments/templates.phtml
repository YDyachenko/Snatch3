<div class="row">
    <div class="col-md-12">
        <div class="page-header">
            <div class="pull-right">
                <a class="btn btn-success iconfix" href="/payments/addTemplate">
                    <span class="glyphicon glyphicon-plus"></span>Add
                </a>
            </div>
            <h1>Payments templates</h1>
        </div>
        <? if (!count($templates)): ?>
            <div class="alert alert-info">You havn't added any template</div>
        <? else: ?>
            <table class="table table-striped transaction-templates">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>From</th>
                        <th>To</th>
                        <th>Sum</th>
                    </tr>
                </thead>
                <tbody>
                    <? foreach ($templates as $template): ?>
                        <tr>
                            <td><?= $this->escapeHtml($template->getName()) ?></td>
                            <td><?
                                $from = $template->getFrom();
                                echo $this->escapeHtml($accounts[$from]->getNumber());
                                ?></td>
                            <td><?= $this->escapeHtml($template->getTo()) ?></td>
                            <td>
                                <div class="pull-right">
                                    <a href="/payments/startFromTemplate/id/<?= $template->getId() ?>" title="Pay" class="show-on-hover"><span class="glyphicon glyphicon-chevron-right"></span></a>
                                    <a href="/payments/editTemplate/id/<?= $template->getId() ?>" title="Edit" class="show-on-hover"><span class="glyphicon glyphicon-edit"></span></a>
                                    <a href="/payments/deleteTemplate/id/<?= $template->getId() ?>" title="Delete" class="show-on-hover"><span class="glyphicon glyphicon-remove"></span></a>
                                </div>
                                <?= $template->getSum() ?> <?= $this->currencySymbol($accounts[$from]->getCurrency()) ?>
                            </td>
                        </tr>
                    <? endforeach; ?>
                </tbody>
            </table>
        <? endif; ?>
    </div>
</div>